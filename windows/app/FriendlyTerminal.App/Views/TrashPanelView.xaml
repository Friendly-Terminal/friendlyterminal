<UserControl
    x:Class="FriendlyTerminal.App.Views.TrashPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="480"
    MinHeight="320">

    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="320" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="SummaryText"
                VerticalAlignment="Center"
                FontSize="12"
                Opacity="0.7" />
            <Button x:Name="EmptyButton" Grid.Column="1" Content="Empty trash">
                <Button.Flyout>
                    <Flyout x:Name="EmptyFlyout">
                        <StackPanel MaxWidth="280" Spacing="8">
                            <TextBlock
                                FontSize="12"
                                Text="This permanently deletes everything in the trash — it can't be undone. Items are also cleared automatically after 30 days."
                                TextWrapping="Wrap" />
                            <Button
                                HorizontalAlignment="Right"
                                Click="OnEmptyConfirmed"
                                Content="Delete forever"
                                Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ItemsPanel" Spacing="2" />
        </ScrollViewer>

        <StackPanel
            x:Name="EmptyStatePanel"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="8"
            Visibility="Collapsed">
            <FontIcon
                HorizontalAlignment="Center"
                FontFamily="{StaticResource SymbolThemeFontFamily}"
                FontSize="24"
                Glyph="&#xE74D;"
                Opacity="0.5" />
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="12"
                Opacity="0.6"
                Text="The trash is empty." />
        </StackPanel>
    </Grid>
</UserControl>
