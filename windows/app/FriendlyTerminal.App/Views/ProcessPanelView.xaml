<UserControl
    x:Class="FriendlyTerminal.App.Views.ProcessPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="460">

    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="340" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                VerticalAlignment="Center"
                FontSize="11"
                Opacity="0.6"
                Text="Programs listening on a network port. Kill stops the program." />
            <ProgressRing
                x:Name="BusyRing"
                Grid.Column="1"
                Width="16"
                Height="16"
                IsActive="False" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ListPanel" Spacing="2" />
        </ScrollViewer>

        <StackPanel
            x:Name="EmptyPanel"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="8"
            Visibility="Collapsed">
            <FontIcon
                HorizontalAlignment="Center"
                FontFamily="{StaticResource SymbolThemeFontFamily}"
                FontSize="28"
                Opacity="0.4"
                Glyph="&#xE945;" />
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="13"
                FontWeight="Medium"
                Opacity="0.7"
                Text="Nothing listening on a port" />
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="11"
                Opacity="0.5"
                Text="Start a dev server and it'll appear here." />
        </StackPanel>
    </Grid>
</UserControl>
